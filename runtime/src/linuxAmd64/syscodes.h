#pragma once

#include "../common/types.h"

const UInt8 SyscodeRead = 0x00;
const UInt8 SyscodeWrite = 0x01;

const UInt8 SyscodeMemoryMap = 0x09;

const UInt8 SyscodeMemoryUnmap = 0x0b;

const UInt8 SyscodeExit = 0x3c;
